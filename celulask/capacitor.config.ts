import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.iepp.celulas.juveniles',
  appName: 'Células Juveniles IPP',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
